name: Test Kaggle Secret

on:
  workflow_dispatch:   # para lanzarlo a mano desde GitHub Actions

jobs:
  test-secret:
    runs-on: ubuntu-latest

    steps:
      - name: Show secret (oculto)
        run: echo "El secreto se cargó correctamente ✅"
